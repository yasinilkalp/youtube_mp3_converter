{
    "name": "youtube-mp3-converter-root",
    "version": "1.0.0",
    "description": "Root script to build and start the application",
    "scripts": {
        "install-deps": "cd client && npm install && cd ../server && npm install",
        "build": "npm run install-deps && cd client && npm run build",
        "start": "node server/index.js"
    },
    "engines": {
        "node": ">=16.0.0"
    },
    "dependencies": {}
}